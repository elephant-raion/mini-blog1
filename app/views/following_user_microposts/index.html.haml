%h1 Following user timeline
- if @feed_items.any?
  %ol.microposts
    = render @feed_items
  = will_paginate @feed_items, previous_label: '&lt 前へ', next_label: '次へ &gt', params: { controller: :following_user_microposts, action: :index }